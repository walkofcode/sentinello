node_modules/
.turbo/
dist/
.next/
.next-production/
.next-production-*/
*.tsbuildinfo
.env
.env.*
!.env.example

data/
*.log
.DS_Store
.woc-ide

# Coverage output from `pnpm test:coverage`; CI uploads it as an artifact instead.
coverage/
# Playwright's own output dirs (tests/e2e results, traces, and the HTML report).
test-results/
playwright-report/

# Generated by `pnpm demo:gen` — throwaway sample projects for exercising the Docker/nvm/audit pipeline.
demo-projects/

docs/superpowers/

# AI assistant / agent config — kept local, not published when open-sourcing.
.llm/
.cursor/
.claude/
AGENTS.md
CLAUDE.md