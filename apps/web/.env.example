# Comma-separated list of cross-origin hostnames allowed to access Next.js dev resources
# (e.g. webpack-hmr). Used by next.config.mjs to populate `allowedDevOrigins`. Dev only.
# Example: NEXT_DEV_ORIGINS=tunnel.example.com
NEXT_DEV_ORIGINS=
