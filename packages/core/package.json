{
    "name": "@sentinello/core",
    "version": "0.1.0",
    "private": true,
    "license": "MIT",
    "description": "Shared domain types for Sentinello (the literal contract between worker and portal).",
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        }
    },
    "scripts": {
        "lint": "eslint src",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    }
}
