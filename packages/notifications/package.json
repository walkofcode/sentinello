{
    "name": "@sentinello/notifications",
    "version": "0.1.0",
    "private": true,
    "license": "MIT",
    "description": "Sentinello notification senders (Slack / Telegram / generic webhook) + render + redact + resolve helpers.",
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./src/index.ts"
        }
    },
    "scripts": {
        "lint": "eslint src",
        "typecheck": "tsc -p tsconfig.json --noEmit"
    },
    "dependencies": {
        "@sentinello/core": "workspace:*",
        "axios": "1.15.2"
    },
    "devDependencies": {
        "@types/node": "25.6.0"
    }
}
