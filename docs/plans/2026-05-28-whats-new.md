# "What's new" pill + version history — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a dismissible "What's new" pill to the top bar that announces the running version's features, plus a `Settings → What's new` page listing every release's highlights.

**Architecture:** A locale-independent index (`release-highlights.ts`, version + date, newest first) drives ordering; all prose lives in the `messages/*.json` catalogs under a `WhatsNew` namespace. A server wrapper resolves the running version and renders a client pill (per-version `localStorage` dismissal + click-outside popover, mirroring `UpdateBannerClient` and `nav-menu.tsx`). The pill is passed into the client `TopNav` as a server-rendered slot prop from `layout.tsx`.

**Tech Stack:** Next.js (App Router, server + client components), next-intl, Tailwind, lucide-react.

**Project conventions that override the skill default:** This repo's `CLAUDE.md` says **never write tests unless asked** — so this plan has **no test tasks**. Each task is implement → verify with `build`/`typecheck`/`lint` → commit. Code style: 4-space indent, single quotes, no semicolons, no trailing comma on last element, `function` keyword (no `const fn = () =>`), prefer `&&`/`||` over ternary, no enums, no `interface`. Match the existing files you touch.

**Note on working tree:** uncommitted MCP/overview changes already exist in the tree. Each commit below uses explicit `git add <paths>` so it captures only that task's files.

---

## File Structure

- **Create** `apps/web/lib/release-highlights.ts` — version+date index and lookup helpers (pure, sync).
- **Modify** all 10 `apps/web/messages/*.json` — add `WhatsNew` namespace, `Nav.tabs.whatsNew`, `Settings.whatsNew.metaTitle`.
- **Create** `apps/web/components/layout/whats-new-pill.tsx` — server wrapper (resolves running version).
- **Create** `apps/web/components/layout/whats-new-pill-client.tsx` — client pill + popover + dismissal.
- **Modify** `apps/web/components/layout/top-nav.tsx` — accept a `whatsNew` slot prop, render it in the right cluster.
- **Modify** `apps/web/app/layout.tsx` — pass `whatsNew={<WhatsNewPill />}` into `<TopNav />`.
- **Create** `apps/web/app/settings/whats-new/page.tsx` — settings route.
- **Create** `apps/web/components/settings/whats-new-content.tsx` — server component listing all releases.
- **Modify** `apps/web/components/settings/settings-tabs.tsx` — register the `whatsNew` tab.
- **Modify** `CLAUDE.md` — document the per-release highlights step.

---

## Task 1: Highlights index module

**Files:**
- Create: `apps/web/lib/release-highlights.ts`

- [ ] **Step 1: Create the module**

```ts
export type ReleaseHighlightMeta = {
    version: string
    date: string
}

// Newest first. Adding an entry here REQUIRES a matching WhatsNew.releases.<version>
// block in EVERY apps/web/messages/*.json — next-intl has no fallback configured, so a
// version present here but missing from a catalog renders an error in that locale.
export const RELEASE_HIGHLIGHTS: ReleaseHighlightMeta[] = [
    { version: '1.4.0', date: '2026-06-01' }
]

function stripVPrefix(value: string): string {
    return value.startsWith('v') && value.slice(1) || value
}

export function getAllHighlights(): ReleaseHighlightMeta[] {
    return RELEASE_HIGHLIGHTS
}

export function getLatestHighlight(): ReleaseHighlightMeta | null {
    return RELEASE_HIGHLIGHTS[0] || null
}

export function getHighlightFor(version: string): ReleaseHighlightMeta | null {
    const bare = stripVPrefix(version)
    return RELEASE_HIGHLIGHTS.find(function match(entry) {
        return entry.version === bare
    }) || null
}
```

- [ ] **Step 2: Typecheck**

Run: `pnpm --filter @sentinello/web typecheck`
Expected: PASS (no errors).

- [ ] **Step 3: Commit**

```bash
git add apps/web/lib/release-highlights.ts
git commit -m "feat(web): add release-highlights index for what's-new"
```

---

## Task 2: i18n — WhatsNew namespace + tab label + meta title in all 10 catalogs

**Files:**
- Modify: `apps/web/messages/{en,de,es,fr,it,ja,ko,pt-BR,ru,zh-CN}.json`

These catalogs already contain (from the MCP work): a `Nav.tabs.mcp` line `            "mcp": "MCP",` and a `        "about": {` object opener in `Settings`, plus a top-level `    "Settings": {`. We insert relative to those exact anchors so formatting is preserved (no full reformat). The script below does first-occurrence string inserts, then validates every file with `JSON.parse`.

- [ ] **Step 1: Write and run the insertion script**

Create a throwaway script `apps/web/scripts/add-whatsnew-i18n.mjs` with this exact content:

```js
import { readFileSync, writeFileSync } from 'node:fs'

const DATA = {
    'en': {
        tab: "What's new",
        meta: "What's new · Settings",
        ns: {
            pillLabel: "What's new",
            popoverHeading: "What's new in v{version}",
            seeFullHistory: 'See full history',
            dismiss: 'Dismiss',
            pageTitle: "What's new",
            pageIntro: 'A summary of what each Sentinello release delivered.',
            releases: {
                '1.4.0': {
                    title: 'MCP integration & what’s-new',
                    items: [
                        'MCP server at /api/mcp for Claude Desktop, Cursor, and other clients',
                        'New Settings → MCP section with server URL and token management',
                        'This what’s-new pill, plus a Settings → What’s new history'
                    ]
                }
            }
        }
    },
    'de': {
        tab: 'Neuigkeiten',
        meta: 'Neuigkeiten · Einstellungen',
        ns: {
            pillLabel: 'Neuigkeiten',
            popoverHeading: 'Neu in v{version}',
            seeFullHistory: 'Vollständige Historie ansehen',
            dismiss: 'Schließen',
            pageTitle: 'Neuigkeiten',
            pageIntro: 'Eine Zusammenfassung dessen, was jede Sentinello-Version gebracht hat.',
            releases: {
                '1.4.0': {
                    title: 'MCP-Integration & Neuigkeiten',
                    items: [
                        'MCP-Server unter /api/mcp für Claude Desktop, Cursor und andere Clients',
                        'Neuer Bereich Einstellungen → MCP mit Server-URL und Token-Verwaltung',
                        'Dieses Neuigkeiten-Symbol sowie eine Historie unter Einstellungen → Neuigkeiten'
                    ]
                }
            }
        }
    },
    'es': {
        tab: 'Novedades',
        meta: 'Novedades · Configuración',
        ns: {
            pillLabel: 'Novedades',
            popoverHeading: 'Novedades en v{version}',
            seeFullHistory: 'Ver historial completo',
            dismiss: 'Descartar',
            pageTitle: 'Novedades',
            pageIntro: 'Un resumen de lo que aportó cada versión de Sentinello.',
            releases: {
                '1.4.0': {
                    title: 'Integración MCP y novedades',
                    items: [
                        'Servidor MCP en /api/mcp para Claude Desktop, Cursor y otros clientes',
                        'Nueva sección Configuración → MCP con URL del servidor y gestión de tokens',
                        'Esta píldora de novedades y un historial en Configuración → Novedades'
                    ]
                }
            }
        }
    },
    'fr': {
        tab: 'Nouveautés',
        meta: 'Nouveautés · Paramètres',
        ns: {
            pillLabel: 'Nouveautés',
            popoverHeading: 'Nouveautés de la v{version}',
            seeFullHistory: "Voir l'historique complet",
            dismiss: 'Fermer',
            pageTitle: 'Nouveautés',
            pageIntro: "Un résumé de ce qu'a apporté chaque version de Sentinello.",
            releases: {
                '1.4.0': {
                    title: 'Intégration MCP et nouveautés',
                    items: [
                        'Serveur MCP sur /api/mcp pour Claude Desktop, Cursor et d’autres clients',
                        'Nouvelle section Paramètres → MCP avec URL du serveur et gestion des jetons',
                        'Cette pastille de nouveautés, ainsi qu’un historique dans Paramètres → Nouveautés'
                    ]
                }
            }
        }
    },
    'it': {
        tab: 'Novità',
        meta: 'Novità · Impostazioni',
        ns: {
            pillLabel: 'Novità',
            popoverHeading: 'Novità nella v{version}',
            seeFullHistory: 'Vedi cronologia completa',
            dismiss: 'Chiudi',
            pageTitle: 'Novità',
            pageIntro: 'Un riepilogo di ciò che ha portato ogni versione di Sentinello.',
            releases: {
                '1.4.0': {
                    title: 'Integrazione MCP e novità',
                    items: [
                        'Server MCP su /api/mcp per Claude Desktop, Cursor e altri client',
                        'Nuova sezione Impostazioni → MCP con URL del server e gestione dei token',
                        'Questa pillola delle novità e una cronologia in Impostazioni → Novità'
                    ]
                }
            }
        }
    },
    'ja': {
        tab: '新着情報',
        meta: '新着情報 · 設定',
        ns: {
            pillLabel: '新着情報',
            popoverHeading: 'v{version} の新着情報',
            seeFullHistory: '全履歴を見る',
            dismiss: '閉じる',
            pageTitle: '新着情報',
            pageIntro: 'Sentinello の各バージョンで追加された内容の概要です。',
            releases: {
                '1.4.0': {
                    title: 'MCP 連携と新着情報',
                    items: [
                        'Claude Desktop、Cursor などのクライアント向けの /api/mcp の MCP サーバー',
                        'サーバー URL とトークン管理を備えた新しい「設定 → MCP」セクション',
                        'この新着情報バッジと「設定 → 新着情報」の履歴'
                    ]
                }
            }
        }
    },
    'ko': {
        tab: '새로운 기능',
        meta: '새로운 기능 · 설정',
        ns: {
            pillLabel: '새로운 기능',
            popoverHeading: 'v{version}의 새로운 기능',
            seeFullHistory: '전체 기록 보기',
            dismiss: '닫기',
            pageTitle: '새로운 기능',
            pageIntro: '각 Sentinello 릴리스가 제공한 내용의 요약입니다.',
            releases: {
                '1.4.0': {
                    title: 'MCP 연동 및 새로운 기능',
                    items: [
                        'Claude Desktop, Cursor 등 클라이언트를 위한 /api/mcp MCP 서버',
                        '서버 URL과 토큰 관리를 갖춘 새로운 설정 → MCP 섹션',
                        '이 새로운 기능 배지와 설정 → 새로운 기능 기록'
                    ]
                }
            }
        }
    },
    'pt-BR': {
        tab: 'Novidades',
        meta: 'Novidades · Configurações',
        ns: {
            pillLabel: 'Novidades',
            popoverHeading: 'Novidades na v{version}',
            seeFullHistory: 'Ver histórico completo',
            dismiss: 'Dispensar',
            pageTitle: 'Novidades',
            pageIntro: 'Um resumo do que cada versão do Sentinello trouxe.',
            releases: {
                '1.4.0': {
                    title: 'Integração MCP e novidades',
                    items: [
                        'Servidor MCP em /api/mcp para Claude Desktop, Cursor e outros clientes',
                        'Nova seção Configurações → MCP com URL do servidor e gerenciamento de tokens',
                        'Esta etiqueta de novidades e um histórico em Configurações → Novidades'
                    ]
                }
            }
        }
    },
    'ru': {
        tab: 'Что нового',
        meta: 'Что нового · Настройки',
        ns: {
            pillLabel: 'Что нового',
            popoverHeading: 'Что нового в v{version}',
            seeFullHistory: 'Посмотреть всю историю',
            dismiss: 'Закрыть',
            pageTitle: 'Что нового',
            pageIntro: 'Сводка того, что появилось в каждой версии Sentinello.',
            releases: {
                '1.4.0': {
                    title: 'Интеграция MCP и новинки',
                    items: [
                        'MCP-сервер по адресу /api/mcp для Claude Desktop, Cursor и других клиентов',
                        'Новый раздел «Настройки → MCP» с URL сервера и управлением токенами',
                        'Этот значок «Что нового» и история в разделе «Настройки → Что нового»'
                    ]
                }
            }
        }
    },
    'zh-CN': {
        tab: '新功能',
        meta: '新功能 · 设置',
        ns: {
            pillLabel: '新功能',
            popoverHeading: 'v{version} 的新功能',
            seeFullHistory: '查看完整历史',
            dismiss: '关闭',
            pageTitle: '新功能',
            pageIntro: 'Sentinello 每个版本带来内容的摘要。',
            releases: {
                '1.4.0': {
                    title: 'MCP 集成与新功能',
                    items: [
                        '面向 Claude Desktop、Cursor 等客户端的 /api/mcp MCP 服务器',
                        '全新的“设置 → MCP”板块，提供服务器 URL 和令牌管理',
                        '此新功能标记，以及“设置 → 新功能”历史记录'
                    ]
                }
            }
        }
    }
}

function indentBlock(value, topKey) {
    // JSON.stringify with 4-space indent, then push every continuation line in by 4
    // more spaces so the namespace sits one level under the root object.
    const json = JSON.stringify(value, null, 4)
    const lines = json.split('\n').map(function pad(line, i) {
        return i === 0 && line || '    ' + line
    })
    return '    "' + topKey + '": ' + lines.join('\n') + ',\n'
}

const dir = new URL('../messages/', import.meta.url)

for (const [locale, data] of Object.entries(DATA)) {
    const path = new URL(locale + '.json', dir)
    let content = readFileSync(path, 'utf8')

    // 1) Nav.tabs.whatsNew — right after the tabs "mcp" label
    const tabAnchor = '            "mcp": "MCP",\n'
    content = content.replace(tabAnchor, tabAnchor + '            "whatsNew": ' + JSON.stringify(data.tab) + ',\n')

    // 2) Settings.whatsNew.metaTitle — right before the Settings "about" object
    const aboutAnchor = '        "about": {\n'
    const metaBlock = '        "whatsNew": {\n            "metaTitle": ' + JSON.stringify(data.meta) + '\n        },\n'
    content = content.replace(aboutAnchor, metaBlock + aboutAnchor)

    // 3) Top-level WhatsNew namespace — right before the top-level "Settings" object
    const settingsAnchor = '    "Settings": {\n'
    content = content.replace(settingsAnchor, indentBlock(data.ns, 'WhatsNew') + settingsAnchor)

    writeFileSync(path, content)
    console.log('updated', locale)
}
```

Run it:

```bash
node apps/web/scripts/add-whatsnew-i18n.mjs
```

Expected: prints `updated en` … `updated zh-CN` (10 lines).

- [ ] **Step 2: Validate every catalog parses and has the keys**

```bash
cd apps/web/messages && for f in en de es fr it ja ko pt-BR ru zh-CN; do node -e "const m=require('./$f.json'); if(!m.WhatsNew?.releases?.['1.4.0']?.items?.length||!m.Nav.tabs.whatsNew||!m.Settings.whatsNew.metaTitle) throw new Error('missing keys in $f'); JSON.stringify(m)" && echo "$f OK"; done; cd -
```

Expected: `en OK` … `zh-CN OK` (10 lines), no thrown errors.

- [ ] **Step 3: Remove the throwaway script**

```bash
rm apps/web/scripts/add-whatsnew-i18n.mjs
```

- [ ] **Step 4: Commit**

```bash
git add apps/web/messages/en.json apps/web/messages/de.json apps/web/messages/es.json apps/web/messages/fr.json apps/web/messages/it.json apps/web/messages/ja.json apps/web/messages/ko.json apps/web/messages/pt-BR.json apps/web/messages/ru.json apps/web/messages/zh-CN.json
git commit -m "i18n(web): add WhatsNew namespace, tab label, and meta title to all locales"
```

---

## Task 3: Settings → What's new page + tab registration

**Files:**
- Create: `apps/web/components/settings/whats-new-content.tsx`
- Create: `apps/web/app/settings/whats-new/page.tsx`
- Modify: `apps/web/components/settings/settings-tabs.tsx`

- [ ] **Step 1: Create the content component**

`apps/web/components/settings/whats-new-content.tsx`:

```tsx
import { getLocale, getTranslations } from 'next-intl/server'
import { getAllHighlights } from '@/lib/release-highlights'

type ReleaseCopy = {
    title: string
    items: string[]
}

export async function WhatsNewContent() {
    const t = await getTranslations('WhatsNew')
    const locale = await getLocale()
    // The version keys contain dots ("1.4.0"), which next-intl would treat as a nested
    // path — so read the whole `releases` object once and index it by version in JS.
    const releases = t.raw('releases') as Record<string, ReleaseCopy>
    const highlights = getAllHighlights()
    return (
        <div className="content mx-auto max-w-3xl space-y-8">
            <header className="space-y-2">
                <h1 className="text-3xl font-bold tracking-tight">{t('pageTitle')}</h1>
                <p className="text-muted-foreground">{t('pageIntro')}</p>
            </header>
            {highlights.map(function renderRelease(meta) {
                const copy = releases[meta.version]
                if (!copy) return null
                const date = new Date(meta.date + 'T00:00:00Z').toLocaleDateString(locale, {
                    dateStyle: 'medium',
                    timeZone: 'UTC'
                })
                return (
                    <section key={meta.version} className="space-y-3 rounded-(--radius-card) border bg-card p-6">
                        <div className="flex items-baseline justify-between gap-3">
                            <h2 className="text-xl font-semibold">{copy.title}</h2>
                            <span className="shrink-0 text-sm text-muted-foreground">v{meta.version} · {date}</span>
                        </div>
                        <ul className="space-y-1.5 text-muted-foreground">
                            {copy.items.map(function renderItem(item, index) {
                                return (
                                    <li key={index} className="flex gap-2">
                                        <span aria-hidden="true" className="mt-2 size-1.5 shrink-0 rounded-full bg-emerald-500" />
                                        <span>{item}</span>
                                    </li>
                                )
                            })}
                        </ul>
                    </section>
                )
            })}
        </div>
    )
}
```

- [ ] **Step 2: Create the route**

`apps/web/app/settings/whats-new/page.tsx`:

```tsx
import type { Metadata } from 'next'
import { getTranslations } from 'next-intl/server'
import { WhatsNewContent } from '@/components/settings/whats-new-content'

export async function generateMetadata(): Promise<Metadata> {
    const t = await getTranslations('Settings')
    return { title: t('whatsNew.metaTitle') }
}

export default function WhatsNewSettingsPage() {
    return <WhatsNewContent />
}
```

- [ ] **Step 3: Register the tab**

In `apps/web/components/settings/settings-tabs.tsx`, the `TABS` array currently ends:

```tsx
    { href: '/settings/advanced', key: 'advanced' },
    { href: '/settings/mcp', key: 'mcp' },
    { href: '/settings/about', key: 'about' }
```

Change it to insert `whatsNew` after `mcp`:

```tsx
    { href: '/settings/advanced', key: 'advanced' },
    { href: '/settings/mcp', key: 'mcp' },
    { href: '/settings/whats-new', key: 'whatsNew' },
    { href: '/settings/about', key: 'about' }
```

- [ ] **Step 4: Typecheck**

Run: `pnpm --filter @sentinello/web typecheck`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add apps/web/components/settings/whats-new-content.tsx apps/web/app/settings/whats-new/page.tsx apps/web/components/settings/settings-tabs.tsx
git commit -m "feat(web): add Settings → What's new version history page"
```

---

## Task 4: The pill (server wrapper + client component)

**Files:**
- Create: `apps/web/components/layout/whats-new-pill.tsx`
- Create: `apps/web/components/layout/whats-new-pill-client.tsx`

- [ ] **Step 1: Create the client component**

`apps/web/components/layout/whats-new-pill-client.tsx`:

```tsx
'use client'

import Link from 'next/link'
import { useEffect, useRef, useState } from 'react'
import { useTranslations } from 'next-intl'
import { Sparkles, X } from 'lucide-react'

// Per-version dismissal: storing the seen version means the pill reappears automatically
// when a newer curated version ships. Mirrors UpdateBannerClient's storage approach.
const STORAGE_KEY = 'sentinello-whatsnew-seen-version'

type ReleaseCopy = {
    title: string
    items: string[]
}

type Props = {
    version: string
}

export function WhatsNewPillClient({ version }: Props) {
    const t = useTranslations('WhatsNew')
    // Start hidden so SSR + first client render emit nothing — no flash for users who
    // already dismissed this version.
    const [show, setShow] = useState(false)
    const [open, setOpen] = useState(false)
    const containerRef = useRef<HTMLDivElement>(null)

    useEffect(function readSeen() {
        try {
            const stored = window.localStorage.getItem(STORAGE_KEY)
            if (stored !== version) setShow(true)
        } catch {
            // localStorage unavailable — show the pill; it just won't stay dismissed
            setShow(true)
        }
    }, [version])

    useEffect(function bindCloseHandlers() {
        if (!open) return
        function onPointerDown(event: PointerEvent) {
            const target = event.target as Node | null
            if (containerRef.current && target && !containerRef.current.contains(target)) {
                setOpen(false)
            }
        }
        function onKeyDown(event: KeyboardEvent) {
            if (event.key === 'Escape') setOpen(false)
        }
        document.addEventListener('pointerdown', onPointerDown)
        document.addEventListener('keydown', onKeyDown)
        return function cleanup() {
            document.removeEventListener('pointerdown', onPointerDown)
            document.removeEventListener('keydown', onKeyDown)
        }
    }, [open])

    function toggleOpen() {
        setOpen(function flip(prev) { return !prev })
    }

    function closePopover() {
        setOpen(false)
    }

    function handleDismiss() {
        try {
            window.localStorage.setItem(STORAGE_KEY, version)
        } catch {
            // best-effort persistence; matches FontSizeProvider's pattern
        }
        setOpen(false)
        setShow(false)
    }

    if (!show) return null

    // Version keys contain dots; read the whole releases object and index by version.
    const releases = t.raw('releases') as Record<string, ReleaseCopy>
    const copy = releases[version]
    if (!copy) return null

    return (
        <div ref={containerRef} className="relative">
            <button
                type="button"
                onClick={toggleOpen}
                aria-haspopup="menu"
                aria-expanded={open}
                className="inline-flex items-center gap-1.5 rounded-md border border-emerald-500/30 bg-emerald-500/10 px-2.5 py-1.5 text-sm font-medium text-emerald-700 transition-colors hover:bg-emerald-500/20 dark:text-emerald-300"
            >
                <Sparkles aria-hidden="true" className="size-4" />
                <span className="hidden sm:inline">{t('pillLabel')}</span>
            </button>
            {open ? (
                <div
                    role="menu"
                    className="absolute right-0 top-full z-40 mt-2 w-72 rounded-md border bg-card p-3 shadow-md"
                >
                    <div className="mb-2 flex items-start justify-between gap-2">
                        <h3 className="text-sm font-semibold">{t('popoverHeading', { version })}</h3>
                        <button
                            type="button"
                            onClick={handleDismiss}
                            aria-label={t('dismiss')}
                            className="rounded p-0.5 text-muted-foreground transition-colors hover:bg-accent hover:text-foreground"
                        >
                            <X aria-hidden="true" className="size-4" />
                        </button>
                    </div>
                    <ul className="space-y-1.5 text-sm text-muted-foreground">
                        {copy.items.map(function renderItem(item, index) {
                            return (
                                <li key={index} className="flex gap-2">
                                    <span aria-hidden="true" className="mt-1 size-1.5 shrink-0 rounded-full bg-emerald-500" />
                                    <span>{item}</span>
                                </li>
                            )
                        })}
                    </ul>
                    <Link
                        href="/settings/whats-new"
                        onClick={closePopover}
                        className="mt-3 inline-block text-sm font-medium text-emerald-700 underline-offset-2 hover:underline dark:text-emerald-300"
                    >
                        {t('seeFullHistory')} →
                    </Link>
                </div>
            ) : null}
        </div>
    )
}
```

- [ ] **Step 2: Create the server wrapper**

`apps/web/components/layout/whats-new-pill.tsx`:

```tsx
import { getHighlightFor } from '@/lib/release-highlights'
import { getCurrentVersion } from '@/lib/version'
import { WhatsNewPillClient } from './whats-new-pill-client'

// Renders nothing when the running version has no curated highlight entry. getCurrentVersion()
// is server-only (reads env / package.json), so this stays a server component and the pill is
// handed to the client TopNav as a slot prop from app/layout.tsx.
export function WhatsNewPill() {
    const current = getCurrentVersion()
    const meta = getHighlightFor(current)
    if (!meta) return null
    return <WhatsNewPillClient version={meta.version} />
}
```

- [ ] **Step 3: Typecheck**

Run: `pnpm --filter @sentinello/web typecheck`
Expected: PASS.

- [ ] **Step 4: Commit**

```bash
git add apps/web/components/layout/whats-new-pill.tsx apps/web/components/layout/whats-new-pill-client.tsx
git commit -m "feat(web): add what's-new pill with dismissible highlights popover"
```

---

## Task 5: Mount the pill in the top bar

**Files:**
- Modify: `apps/web/components/layout/top-nav.tsx`
- Modify: `apps/web/app/layout.tsx`

- [ ] **Step 1: Add the slot prop to TopNav**

In `apps/web/components/layout/top-nav.tsx`:

First add `type ReactNode` to the React import. The current import is:

```tsx
import { useEffect, useRef, useState } from 'react'
```

Change to:

```tsx
import { useEffect, useRef, useState, type ReactNode } from 'react'
```

Then change the component signature from:

```tsx
export function TopNav() {
```

to:

```tsx
export function TopNav({ whatsNew }: { whatsNew?: ReactNode }) {
```

- [ ] **Step 2: Render the slot in the right-side cluster**

In the same file, the right-side cluster currently begins:

```tsx
                <div className="relative ml-auto flex items-center gap-2">
                    {mobileLabel ? (
                        <span className="max-w-[40vw] truncate rounded-md bg-accent/50 px-2 py-1 text-xs font-semibold uppercase tracking-wider text-accent-foreground md:hidden">
                            {mobileLabel}
                        </span>
                    ) : null}
                    <Button
```

Insert `{whatsNew}` between the `mobileLabel` span and the `<Button>`:

```tsx
                <div className="relative ml-auto flex items-center gap-2">
                    {mobileLabel ? (
                        <span className="max-w-[40vw] truncate rounded-md bg-accent/50 px-2 py-1 text-xs font-semibold uppercase tracking-wider text-accent-foreground md:hidden">
                            {mobileLabel}
                        </span>
                    ) : null}
                    {whatsNew}
                    <Button
```

- [ ] **Step 3: Pass the pill from the layout**

In `apps/web/app/layout.tsx`, add the import after the `UpdateBanner` import (line 9):

```tsx
import { WhatsNewPill } from '@/components/layout/whats-new-pill'
```

Then change `<TopNav />` (line 43) to:

```tsx
                                <TopNav whatsNew={<WhatsNewPill />} />
```

- [ ] **Step 4: Build (verifies the server-slot-into-client-component wiring compiles and renders)**

Run: `pnpm --filter @sentinello/web build`
Expected: PASS; route list includes `/settings/whats-new`.

- [ ] **Step 5: Commit**

```bash
git add apps/web/components/layout/top-nav.tsx apps/web/app/layout.tsx
git commit -m "feat(web): mount what's-new pill in the top bar via layout slot"
```

---

## Task 6: Document the per-release highlights step

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Add a subsection to the release-engineering section**

In `CLAUDE.md`, find the "Release engineering" section. Immediately after the "Hard rules" list (the bullet ending with "This is a silent footgun."), insert this new block:

```markdown
### "What's new" highlights (manual, per release)

The top-bar **"What's new" pill** and **Settings → What's new** page are curated, not auto-generated. When you cut a release, do BOTH of these in the release PR (or the commit that finalizes the version):

1. Add a `{ version, date }` entry (newest first) to `apps/web/lib/release-highlights.ts`.
2. Add a matching `WhatsNew.releases.<version>` block (`title` + `items` array) to **every** `apps/web/messages/*.json` (all 10 locales).

next-intl has **no fallback** configured — a version listed in `release-highlights.ts` but missing from a catalog renders an error in that locale. Index and catalogs must stay in lockstep. The pill only appears when the running version has a matching index entry; if you skip the highlights for a release, no pill shows (the update-available banner is unaffected).
```

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: document per-release what's-new highlights step"
```

---

## Task 7: Final verification

**Files:** none (verification only)

- [ ] **Step 1: Full build, lint, typecheck**

```bash
pnpm build && pnpm lint && pnpm typecheck
```

Expected: all tasks succeed; `/settings/whats-new` appears in the web route list.

- [ ] **Step 2: Manual smoke test (report results to Betty; do not assert without running)**

Start dev faking the shipping version so the pill triggers:

```bash
SENTINELLO_VERSION=1.4.0 pnpm dev
```

Verify:
- Top bar shows the "✨ What's new" pill (icon-only under the `sm` breakpoint).
- Clicking it opens a popover with the 1.4.0 heading + 3 bullets + "See full history →".
- Clicking outside / Escape closes the popover; it does NOT mark seen (pill still there on reload).
- The X / dismiss inside the popover hides the pill; after reload it stays hidden.
- `/settings/whats-new` (and the new "What's new" settings tab) lists v1.4.0 with its date and bullets.
- Switch locale via the top menu → pill label, popover, and page render translated copy.
- Without the env override (`pnpm dev`, running version 1.3.1, which has no index entry) → no pill; `/settings/whats-new` still renders the 1.4.0 entry.
- To re-test dismissal: clear `localStorage['sentinello-whatsnew-seen-version']` in DevTools.

---

## Self-review notes

- **Spec coverage:** highlights index (Task 1), localized prose + tab/meta keys (Task 2), settings history page + tab (Task 3), pill server+client with per-version dismissal and click-outside popover (Task 4), top-bar mount via slot (Task 5), CLAUDE.md release step (Task 6). All spec sections covered.
- **Dots-in-keys footgun:** both the client pill and the settings content read `t.raw('releases')` and index by version in JS rather than using a dotted `releases.1.4.0.x` path — documented inline in both files.
- **Server/client boundary:** the pill is server-only (`getCurrentVersion`) and reaches the client `TopNav` only as a slot prop rendered by the server `layout.tsx` — never imported into the client component. Covered explicitly in Task 5.
- **Type consistency:** `ReleaseHighlightMeta` (Task 1), `ReleaseCopy` (Tasks 3 & 4) use identical shapes; helper names `getAllHighlights`/`getHighlightFor`/`getLatestHighlight` and storage key `sentinello-whatsnew-seen-version` are consistent across tasks.
- **No tests:** per `CLAUDE.md` ("never write tests unless asked"); verification is build/lint/typecheck + a manual checklist.
